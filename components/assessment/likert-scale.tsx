"use client"

interface LikertScaleProps {
  questionId: string
  questionText: string
  value: number | null
  onChange: (value: number) => void
}

export default function LikertScale(_props: LikertScaleProps) {
  return null
}
