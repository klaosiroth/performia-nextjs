import type { PillarKey } from "@/tokens/pillars"

interface RecommendedFocusProps {
  pillars: PillarKey[]
  lang?: "th" | "en"
}

export default function RecommendedFocus(_props: RecommendedFocusProps) {
  return null
}
